import { initializeApp } from "https://www.gstatic.com/firebasejs/12.1.0/firebase-app.js";
import { getFirestore, collection, onSnapshot } from "https://www.gstatic.com/firebasejs/12.1.0/firebase-firestore.js";
import { firebaseConfig } from "./firebase-config.js";
const app=initializeApp(firebaseConfig,{name:"homework-hub-schedule-view"});
const db=getFirestore(app); const $=id=>document.getElementById(id);
const slots=[{key:"p1",label:"Tiết 1"},{key:"p2",label:"Tiết 2"},{key:"p3",label:"Tiết 3"},{key:"p4",label:"Tiết 4"},{key:"lunch",label:"Nghỉ trưa"},{key:"p5",label:"Tiết 5"},{key:"p6",label:"Tiết 6"},{key:"p7",label:"Tiết 7"}];
const days=["Thứ 2","Thứ 3","Thứ 4","Thứ 5","Thứ 6"];
const esc=v=>String(v??"").replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
const clean=v=>String(v??"").trim();
function normalize(raw,id){const x=raw||{};return {id,date:clean(x.date||id.split("_")[0]),slot:clean(x.slot||id.split("_").slice(1).join("_")),subject:clean(x.subject||x.subjectName||x.course||x.courseName||x.mon||x.monHoc||x.name),lesson:clean(x.lesson||x.content||x.description||x.topic||x.noiDung||x.noi_dung),teacher:clean(x.teacher||x.teacherName||x.tutor||x.giaoVien||x.giao_vien),note:clean(x.note||x.notes||x.reminder||x.danDo||x.dan_do)};}
function pad(n){return String(n).padStart(2,"0")} function keyDate(d){return `${d.getFullYear()}-${pad(d.getMonth()+1)}-${pad(d.getDate())}`}
function monday(d){let x=new Date(d);x.setHours(12,0,0,0);let n=x.getDay();x.setDate(x.getDate()+(n===0?-6:1-n));return x}
function addDays(d,n){let x=new Date(d);x.setDate(x.getDate()+n);return x}
function todayList(map){const today=keyDate(new Date());const arr=[...map.values()].filter(x=>x.date===today && (x.subject||x.lesson||x.teacher||x.note)).sort((a,b)=>(slots.findIndex(s=>s.key===a.slot)-slots.findIndex(s=>s.key===b.slot)));const c=$("classScheduleTodayList"),b=$("bmeClassScheduleList");if(!c&&!b)return;const out=arr.length?arr.map(x=>`<article class="schedule-today-card"><div class="schedule-slot">${esc(slots.find(s=>s.key===x.slot)?.label||x.slot)}</div><div class="schedule-body"><div class="schedule-subject-label">MÔN HỌC</div><b>${esc(x.subject||"Chưa ghi môn")}</b>${x.lesson?`<p><strong>📖 Nội dung:</strong> ${esc(x.lesson)}</p>`:""}${x.teacher?`<p><strong>👨‍🏫 Thầy cô:</strong> ${esc(x.teacher)}</p>`:""}${x.note?`<p><strong>💬 Dặn dò:</strong> ${esc(x.note)}</p>`:""}</div></article>`).join(""):"<div class=\"schedule-no-data\">📭 Hôm nay chưa có nội dung.</div>"};if(c)c.innerHTML=out;if(b)b.innerHTML=out}
function bmeWeekTable(map,weekStart){const c=$("bmeScheduleTable");if(!c)return;let html='<div class="schedule-table-wrap"><table class="class-schedule-table"><thead><tr><th>Ngày</th>'+slots.map(s=>`<th>${esc(s.label)}</th>`).join("")+'</tr></thead><tbody>';for(let i=0;i<5;i++){const d=addDays(weekStart,i),date=keyDate(d);html+=`<tr><th><b>${days[i]}</b><small>${esc(d.toLocaleDateString("vi-VN",{day:"2-digit",month:"2-digit"}))}</small></th>`;for(const sl of slots){const x=map.get(`${date}_${sl.key}`);html+=`<td>${x&&(x.subject||x.lesson||x.teacher||x.note)?`<div class="bme-schedule-cell"><b>${esc(x.subject||"Chưa ghi môn")}</b>${x.lesson?`<span>${esc(x.lesson)}</span>`:""}${x.teacher?`<small>👨‍🏫 ${esc(x.teacher)}</small>`:""}${x.note?`<small>💬 ${esc(x.note)}</small>`:""}</div>`:'<span class="schedule-empty">—</span>'}</td>`}html+='</tr>'}html+='</tbody></table></div>';c.innerHTML=html}
const map=new Map();let bmeWeek=monday(new Date());
function render(){todayList(map);bmeWeekTable(map,bmeWeek);const label=$("bmeScheduleWeekLabel");if(label)label.textContent=`${bmeWeek.toLocaleDateString("vi-VN",{day:"2-digit",month:"2-digit"})} – ${addDays(bmeWeek,4).toLocaleDateString("vi-VN",{day:"2-digit",month:"2-digit",year:"numeric"})}`}
onSnapshot(collection(db,"class_schedule"),snap=>{map.clear();snap.docs.forEach(d=>map.set(d.id,normalize(d.data(),d.id)));render()},err=>{console.error("Homework Hub class schedule:",err);$("classScheduleTodayList")?.replaceChildren(Object.assign(document.createElement("div"),{className:"schedule-no-data",textContent:"❌ Không thể tải thời khóa biểu."}));});
$("bmeSchedulePrev")?.addEventListener("click",()=>{bmeWeek=addDays(bmeWeek,-7);render()});$("bmeScheduleNext")?.addEventListener("click",()=>{bmeWeek=addDays(bmeWeek,7);render()});$("bmeScheduleToday")?.addEventListener("click",()=>{bmeWeek=monday(new Date());render()});render();
